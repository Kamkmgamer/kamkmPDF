# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Drizzle
DATABASE_URL="postgresql://postgres:password@localhost:5432/pdfprompt"

# Clerk Auth (required for authentication)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_your-clerk-publishable-key-here"
CLERK_SECRET_KEY="sk_test_your-clerk-secret-key-here"

# Application URL (required for share link generation)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Public Site URL (used for canonical URLs, sitemap, robots)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# OpenRouter (optional but required for AI HTML generation)
# Sign up at https://openrouter.ai/ and create an API key. In development, ensure
# NEXT_PUBLIC_APP_URL is set to your local dev URL.
OPENROUTER_API_KEY="sk-or-v1_xxx"
# Optional: override base URL
# OPENROUTER_BASE_URL="https://openrouter.ai/api/v1"

# Puppeteer (optional) — when running in restricted environments you may need to set an executable path.
# PUPPETEER_EXECUTABLE_PATH="C:\Program Files\Google\Chrome\Application\chrome.exe"

# Resend (for contact form)
# Sign up at https://resend.com/ and create an API key.
# You will also need to verify a domain to send emails from.
RESEND_API_KEY="re_your-resend-api-key-here"
CONTACT_RECEIVER_EMAIL="your-email@example.com"
RESEND_FROM_EMAIL="noreply@your-verified-domain.com"

# Example:
# SERVERVAR="foo"
# NEXT_PUBLIC_CLIENTVAR="bar"

# UploadThing
UPLOADTHING_TOKEN=

# Worker (optional, for Vercel cron-driven drain)
# How many jobs to process per invocation (default 5)
# PDFPROMPT_MAX_JOBS_PER_INVOCATION=5
# How many jobs to claim at once per DB roundtrip (default 5)
# PDFPROMPT_BATCH_SIZE=5
# Max milliseconds per invocation before exiting (default 55000)
# PDFPROMPT_MAX_MS_PER_INVOCATION=55000
# Optional shared secret to protect /api/worker/drain (leave unset when using Vercel Cron)
# PDFPROMPT_WORKER_SECRET="your-strong-secret"
